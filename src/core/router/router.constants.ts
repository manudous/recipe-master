const baseRoutes = {
  root: "/",
  recipes: "/recipes",
  inventory: "/inventory",
  favorites: "/favorites",
  profile: "/profile",
};

export const appRoutes = {
  root: baseRoutes.root,
  recipes: baseRoutes.recipes,
  inventory: baseRoutes.inventory,
  favorites: baseRoutes.favorites,
  profile: baseRoutes.profile,
};
